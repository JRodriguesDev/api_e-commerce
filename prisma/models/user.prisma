model User {
    id String @id @default(uuid())
    name String
    email String @unique
    password String
    role Role @default(USER)

    cart Cart?
    products Product[]
    reviews Reviews[]

    stripe_profile Profile?

    @@map("users")
}

enum Role {
    USER
    ADMIN
}