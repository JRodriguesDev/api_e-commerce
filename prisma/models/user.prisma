model User {
    id String @id @default(uuid())
    name String
    email String @unique
    password String
    role Role @default(USER)

    products Product[]
    reviews Reviews[]
    
    @@map("users")
}

enum Role {
    USER
    ADMIN
}